@page "/statuses/"

<h3>Statuses</h3>

<div class="m-2 d-flex justify-content-end">
    <a href="/statuses/create" class="btn btn-success">Create</a>
</div>

@if (string.IsNullOrEmpty(message) == false)
{
    <div class="alert alert-danger">
        @message
    </div>
}

<div>
    @if (statuses == null)
    {
        <p>Loading Data...</p>
    }
    else
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var status in statuses)
                {
                    <tr>
                        <td>@status.StatusID </td>
                        <td>@status.Description </td>
                        <td>
                            <AuthorizeView Roles="Administrator">
                                <button @onclick="@(() => EditStatus(status.StatusID))" class="btn btn-warning">Edit</button>
                                <button @onclick="@(() => DeleteStatus(status.StatusID))" class="btn btn-danger">Delete</button>
                            </AuthorizeView>
                            <button @onclick="@(() => DetailsStatus(status.StatusID))" class="btn btn-info">Details</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>